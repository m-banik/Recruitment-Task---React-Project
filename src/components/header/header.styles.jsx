import styled from "styled-components";

export const StyledHeader = styled.header`
  font-size: 1.5rem;
  font-weight: 700;
  text-align: center;
  letter-spacing: 1px;
  &.login-header {
    margin-bottom: 30px;
  }
`;
