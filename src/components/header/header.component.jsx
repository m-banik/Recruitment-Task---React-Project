import React from "react";
import { StyledHeader } from "./header.styles";

const Header = ({ classNames }) => (
  <StyledHeader className={classNames}>Simple Web App</StyledHeader>
);

export default Header;
